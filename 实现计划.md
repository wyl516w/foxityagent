# 实现计划

最后更新：2026-04-13

## 0. 维护约定

- 每次代码修改都要同步更新 `checklog.md`
- 每次影响产品入口、交互方式或能力边界的修改，都要同步更新 `用户手册.md`
- 每完成一个可提交步骤，都要提交并 push 到 GitHub

## 1. 产品方向

项目已经正式从“工作流编排 / 手工定义任务流原型”转向“以对话为核心入口的桌面自动化 Agent 工作台”。

当前默认原则：

1. 用户通过聊天直接表达目标，不再以手工创建 workflow / step / task tree 作为主入口
2. workflow、task、agent、approval 继续保留，但主要作为后端内部执行状态机
3. 系统围绕 conversation / session 组织自动化，而不是围绕 workflow definition 组织
4. 高风险动作继续保留确认与审查能力，但要以内联确认的方式嵌入对话流程
5. 主界面只保留聊天、当前会话执行态、Agent 状态、事件和必要确认入口

## 2. 当前主链路

后续开发继续优先收敛到这 6 条主链路：

1. `chat / session`
2. `agent orchestrator`
3. `perception`
4. `desktop controller / system executor`
5. `memory / persistence`
6. `permission & safety`

当前不再优先投入：

- 面向普通用户的 workflow editor
- 通用 BPM / 流程引擎式产品形态
- 让用户维护节点、分支、审批图
- 以脚本编写作为主要使用方式

## 3. 阶段 G：稳定性治理与契约收口

目标：先把现有实现里的重复定义、契约错位、事件噪音和测试漂移收掉，恢复一个可靠的“对话主入口”基础版本。

### 已完成

#### G / 1

- 重写计划，正式把“代码治理意见”和“产品重心转向对话式 Agent”纳入主路线

#### G / 2

- 重写 `src/agent_studio/ui/main_window.py`
- 移除重复方法定义和 workflow-first UI 残留
- 主界面收敛为：
  - 左侧会话列表 + 聊天历史 + 图片附件 + 输入区
  - 右侧当前会话任务标签 + Agent 标签 + 内联确认 + 最近事件
- 去掉任务编排面板、感知面板、脚本面板和多余子窗口
- 加入请求去重，避免定时刷新不断堆积线程
- 修复聊天失败时丢失输入内容的问题
- 清理 `src/agent_studio/ui/i18n.py` 里的旧文案和乱码
- 补齐 `/api/chat` 在启用 workflow service 时的任务响应组装

#### G / 3

- 解决 `git pull` 后的文档冲突，并继续保持本地“对话式 Agent 工作台”方向
- `/api/chat` 改为继承当前设置页里的 provider / model / base_url，不再写死到本地 Ollama
- `BackendServer.start()` 改为真正 fail fast，后端未就绪时直接抛错
- `SharedState` 和 `SQLiteStore` 收口事件写入逻辑，移除泛化的 UI 状态噪音事件，并避免每次事件写入后的数据库回读
- 权限决策真正落库到 `permission_audit`
- 会话任务查询下推到 SQLite，避免原来的 Python 过滤和 conversation 详情 N+1 读取
- 修复系统脚本准备相关测试的 async 漂移，并补充 fail-fast / 权限审计 / 聊天继承模型设置的回归测试

### 下一步

#### G / 4

继续收口 chat / session 与右侧执行态之间的剩余契约，重点处理：

1. 让“会话右侧状态区”更像 conversation 的执行投影，而不是 workflow 结构暴露
2. 继续弱化普通用户可见的 task / workflow 术语，仅保留 Agent 状态和必要执行细节
3. 统一会话消息、执行状态、审批状态、最近事件之间的展示节奏
4. 为后续真正的 agent loop 预留更清晰的 session 内存和上下文入口

## 4. 阶段 H：对话式 Agent 工作台

目标：彻底完成“聊天即任务、任务围绕会话、workflow 对用户隐身”的产品形态。

核心工作：

1. 建立真正的 conversation-centered agent loop
2. 让模型自己决定何时截屏、OCR、定位、控制桌面和发起子 Agent
3. 让失败重试、卡点说明、人工接管和继续对话修正形成闭环
4. 将高风险确认完全嵌入聊天与执行流，不再依赖旧流程式界面

验收标准：

- 用户只通过聊天就能发起并完成一个桌面任务
- 用户不需要手工创建 workflow
- 系统能连续执行多个感知 / 动作回合
- 高风险动作会暂停并请求确认
- 用户能在一个会话里看到完整执行过程，并继续修正任务

## 5. 当前阶段状态

当前已经执行到：

- 阶段 G / 第 3 步：稳定性治理与契约收口

下一步建议直接进入：

- 阶段 G / 第 4 步：把右侧工作区进一步收敛成“当前会话执行态”，继续隐藏 workflow 内部细节
